function Button({ label, onClick }) {
  return (
     <button style={{ background: '#b18d70', color: 'white', border: 'none', borderRadius: 20, padding: '10px 20px', cursor: 'pointer' }}>
          Get Started Now
        </button>
  );
}
export default Button;